export type MenuItemProps = {
  text?: string;
  selectCategory?: Function;
}

export type ProductItemProps = {
  text?: string;
  selectCategory?: Function;
}

export type GenderProps = {
  text?: string;
  selectCategory?: Function;
}
